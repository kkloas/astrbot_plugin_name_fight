# Balance Model

这份文档用于说明 `Name Fight` 当前版本的战斗数值机制，并给出一套更贴近实际引擎的武学评估模型。

目标有两个：

- 让没有接触过项目的人，也能快速理解当前战斗系统
- 给后续平衡性讨论提供统一口径，避免只看单一公式得出偏差结论

## 1. 适用范围

本文讨论的是当前项目内的实际战斗逻辑，不是纯设计草案。

相关实现文件：

- `engine.py`: 行动条、出手顺序、命中、伤害、状态结算
- `database.py`: 角色基础属性生成与武学 / 内功 / 轻功修正
- `configs/martial_arts.json`: 武学配置
- `configs/neigong.json`: 内功配置
- `configs/qinggong.json`: 轻功配置

## 2. 当前战斗机制总览

### 2.1 角色属性来源

角色先生成基础六维：

- `hp`
- `atk`
- `def`
- `spd`
- `crt`
- `eva`

然后叠加三类系统：

- 武学：修正 `atk / spd / crt / eva`
- 内功：修正 `hp / def`，并提供 `part_guard` 和被动
- 轻功：修正 `spd`，并直接增加 `eva`

角色最终战斗面板来自：

```text
FinalStats
= BaseStats
+ MartialArtModifiers
+ NeigongModifiers
+ QinggongModifiers
```

更准确地说：

```text
atk = base_atk x martial.atk
def = base_def x neigong.def_multiplier
spd = base_spd x martial.spd x qinggong.spd_multiplier
crt = base_crt x martial.crt
eva = base_eva x martial.eva + qinggong.eva_bonus
hp  = base_hp x neigong.hp_multiplier x HP_BATTLE_SCALE
```

其中 `HP_BATTLE_SCALE = 2.2`，说明项目故意把气血拉高，使战斗不会因为单手高伤而过快结束。

## 2.2 出手顺序不是轮流制，而是行动条制

每个 tick，双方都会累积行动值：

```text
ag += effective_spd
```

当 `ag >= 100` 时才有资格出手。

这意味着：

- `spd` 不只是“更快一点”
- `spd` 直接决定单位时间内能出多少手
- `slowed` 这种减速状态，本质上是在压缩敌人的行动次数

所以速度是当前系统的第二核心属性，而不是边缘属性。

## 2.3 一次攻击的实际流程

一次出手的顺序是：

1. 处理回合开始效果
2. 如果是 `stunned`，直接空过本手
3. 如果武器脱手，先拾回武器，本手空过
4. 随机选择一招
5. 先判目标闪避
6. 如果命中，决定命中部位
7. 计算直接伤害
8. 判暴击
9. 结算内功被动
10. 结算招式附带状态

这里最重要的三个点：

- 闪避在伤害之前生效，所以 `eva` 会完整取消整次攻击
- 命中部位会直接改变伤害倍率
- 状态不是装饰，它们会改变之后的行动效率和生存能力

## 2.4 当前项目里的部位机制

基础部位和倍率如下：

| 部位 | 基础权重 | 伤害倍率 |
|---|---:|---:|
| `head` | 0.13 | 1.35 |
| `chest` | 0.24 | 1.20 |
| `arm` | 0.21 | 0.90 |
| `abdomen` | 0.24 | 1.00 |
| `leg` | 0.18 | 0.85 |

这意味着：

- 打头和打胸，长期收益显著更高
- 打臂和打腿，基础直伤偏低，但更适合挂状态
- `target_bias` 和 `target_weights` 不是风味字段，而是实打实的输出修正

## 2.5 当前项目里的状态机制

当前武学主要涉及这几类状态：

- `stunned`: 目标下回合空过
- `disarmed`: 目标武器脱手，下回合先捡武器，等价于空过一手
- `bleeding`: 回合开始掉血
- `weakened`: 降低目标攻击
- `slowed`: 降低目标速度
- `armor_broken`: 降低目标防御

它们并不等价：

- `stunned / disarmed` 是硬控制，收益最高
- `weakened / slowed` 更偏节奏压制和防守收益
- `armor_broken` 更偏进攻放大
- `bleeding` 是持续伤害，不算控制，但会显著拉高长期输出

## 3. 为什么旧公式不够用

旧公式：

```text
TPS = PSM x (BAM + EEV)
```

它能做一个粗略初筛，但不足以描述当前项目的真实战斗，原因有五个：

1. 没有把 `target_bias / target_weights` 纳入，忽略了部位命中的长期收益差
2. 没有体现行动条机制，无法正确体现 `spd` 的真实作用
3. 没有体现 `eva` 的“整段取消攻击”特性
4. 没有体现 `disarmed` 的真实语义，它在当前引擎里近似等于一手硬控
5. 没有体现内功的 `part_guard / regeneration / thorns / vampirism` 对战斗环境的影响

因此，旧公式适合做“静态直观比较”，不适合做当前版本的平衡判断。

## 4. 修正版武学评估模型

### 4.1 建模目标

新模型不尝试精确替代模拟器，而是追求三件事：

- 和真实引擎方向一致
- 能解释为什么某武学强
- 能拆出直伤、节奏、控制、生存联动四个维度

### 4.2 参考环境

为了让不同武学能放在同一尺子上比较，本文使用一套中性参考面板：

| 参数 | 参考值 |
|---|---:|
| `ATK_ref` | 70 |
| `DEF_ref` | 55 |
| `SPD_ref` | 45 |
| `CRT_ref` | 18 |
| `EVA_ref` | 18 |

这组值不是任意拍的，而是按当前角色生成区间取的中段代表值。

### 4.3 直伤期望

对每个招式，先计算其平均部位倍率：

```text
PartEV_i
= sum over parts(
    P(part | martial_art, move_i) x part_damage_multiplier
  )
```

其中：

- 如果招式写的是 `target_weights`，就按显式权重计算
- 如果招式写的是 `target_bias`，就从全局部位权重乘上武学偏向和招式偏向

再计算单招直伤期望：

```text
HitRate = 1 - EVA_ref / 100
CritFactor = 1 + CRT_ref x crt_mod / 100

BaseHit_i
= max(
    ATK_ref x atk_mod x move_mult_i x PartEV_i - 0.5 x DEF_ref,
    0.1 x ATK_ref x atk_mod
  )

DirectDamageEV_i = HitRate x CritFactor x BaseHit_i + BleedEV_i
```

其中：

- `move_mult_i` 来自 `damage_multiplier`，或者 `(power_min + power_max) / 2`
- 方差 `0.92 ~ 1.08` 的期望取 1.0
- `bleeding` 被计入直伤维度，因为它本质是持续伤害

### 4.4 控制等价值

为了比较不同控制，把所有非流血状态都折成“伤害等价值”。

```text
ControlEV_i
= StunEV_i
+ DisarmEV_i
+ WeakenEV_i
+ SlowEV_i
+ ArmorBreakEV_i
```

各项定义如下：

```text
StunEV
= chance x duration x EnemyDamagePerAction_ref

DisarmEV
= chance x duration x EnemyDamagePerAction_ref

WeakenEV
= chance x duration x EnemyDamagePerAction_ref x (1 - atk_multiplier)

SlowEV
= chance x duration x EnemyDamagePerAction_ref x (1 - spd_multiplier) x 0.85

ArmorBreakEV
= chance x duration x 0.5 x DEF_ref x (1 - def_multiplier)
```

其中：

- `EnemyDamagePerAction_ref` 是参考敌方每次有效出手的期望伤害
- `part_bonus` 会和 `chance` 一起折入期望触发率

### 4.5 节奏因子

因为当前是行动条系统，所以武学速度修正直接参与总评分：

```text
TempoFactor = spd_mod
```

这不是最终行动次数的完整模拟，但能抓住当前版本最关键的节奏方向。

### 4.6 生存联动

生存联动不是“这个武学防御高不高”，而是“这个武学通过自身机制，能帮角色少挨多少伤害”。

本文把它拆成两部分：

```text
SurvivalSynergy
= DodgePreventedEV + DefensiveControlEV
```

其中：

```text
DodgePreventedEV
= EnemyDamagePerAction_ref x extra_eva_rate
```

`extra_eva_rate` 指武学对 `eva` 的额外放大量。

`DefensiveControlEV` 则只统计这些效果对敌方输出的压制：

- `stunned`
- `disarmed`
- `weakened`
- `slowed`

`armor_broken` 不计入生存联动，因为它是纯进攻收益。

### 4.7 总评分

最终用于武学排序的原始评分：

```text
MartialScore_raw
= TempoFactor x (AvgDirectDamageEV + AvgControlEV)
```

其中：

- `AvgDirectDamageEV` 是全招式的平均直伤期望
- `AvgControlEV` 是全招式的平均控制等价值

注意：

- `生存联动` 是单独展示的画像维度，不直接并入总分
- 原因是它和控制的防守价值有重叠，直接相加会双重计权

## 5. 属性重要性结论

为了判断基础属性谁更重要，使用当前引擎做边际提升测试。

基准面板：

```text
hp 330 / atk 70 / def 55 / spd 45 / crt 18 / eva 18
```

测试方法：

- 取实际流派组合样本进行对战
- 每个样本重复多个随机种子
- 每次只提升一个属性，看胜率增量

测试结果：

| 属性 | 典型增量 | 胜率提升 |
|---|---:|---:|
| `atk` | +9 | +6.72% |
| `spd` | +5 | +4.27% |
| `eva` | +4 | +2.03% |
| `def` | +8 | +1.93% |
| `hp` | +35 | +1.82% |
| `crt` | +4 | +0.52% |

归一化后的属性权重：

| 属性 | 重要权重 |
|---|---:|
| `atk` | 38.9% |
| `spd` | 24.7% |
| `eva` | 11.7% |
| `def` | 11.1% |
| `hp` | 10.5% |
| `crt` | 3.0% |

### 5.1 如何理解这组权重

- `atk` 是第一属性，因为它同时抬高直伤、抬高最低保底伤害、抬高流血来源伤害
- `spd` 是第二属性，因为它决定单位时间内的出手次数
- `eva` 比 `crt` 重要得多，因为它会整段取消一次攻击
- `def` 和 `hp` 偏稳定收益，属于“更能扛”但不太会改变节奏
- `crt` 当前最弱，因为它只在命中后才参与，而且只是概率翻倍

如果后续要调平衡，最值得关注的不是暴击，而是：

- 高攻击武学是不是倍率过高
- 高速度武学是不是拿到了过多控制
- 闪避堆叠是否过强

## 6. 九门武学评分总表

说明：

- `Raw` 是修正版模型下的原始排序分
- 四个分项为标准化画像分，范围 `0 ~ 100`
- 标准化只用于横向比较，不代表绝对强度

| 排名 | 武学 | Raw | 直伤 | 节奏 | 控制 | 生存联动 |
|---|---|---:|---:|---:|---:|---:|
| 1 | 华山剑法 | 79.89 | 100.0 | 36.0 | 0.0 | 0.0 |
| 2 | 岳家枪法 | 75.30 | 78.4 | 40.0 | 21.5 | 10.5 |
| 3 | 北辰一刀流 | 72.36 | 68.4 | 40.0 | 26.2 | 18.5 |
| 4 | 乘云刀法 | 67.33 | 57.3 | 40.0 | 10.3 | 0.0 |
| 5 | 碎浪掌 | 65.54 | 55.4 | 0.0 | 78.2 | 73.3 |
| 6 | 落梅剑法 | 63.38 | 19.5 | 52.0 | 91.2 | 85.4 |
| 7 | 逍遥无相剑 | 63.17 | 9.6 | 100.0 | 55.1 | 64.9 |
| 8 | 掌命判官笔 | 59.78 | 16.5 | 60.0 | 49.2 | 46.4 |
| 9 | 追影腿 | 59.61 | 0.0 | 68.0 | 100.0 | 100.0 |

## 7. 九门武学逐个拆解

### 7.1 华山剑法

画像：

- 直伤第一
- 节奏中上
- 几乎没有控制
- 完全靠纯倍率和优质部位分布取胜

结论：

- 这是当前版本最标准的“纯输出模板”
- 强点不在状态，而在招式倍率高且打点稳定
- 如果未来项目要设定一个“高直伤但低功能”的基准线，华山剑法可以作为参照

代表招式：

| 招式 | 平均部位倍率 | 直伤 EV | 控制 EV |
|---|---:|---:|---:|
| 白云出岫 | 1.200 | 60.00 | 0.00 |
| 白虹贯日 | 1.200 | 86.04 | 0.00 |
| 无边落木 | 1.200 | 94.73 | 0.00 |
| 青山隐隐 | 1.200 | 103.41 | 0.00 |

### 7.2 岳家枪法

画像：

- 直伤很高
- 节奏中上
- 有一定减速和破甲补充
- 是比较完整的压制型高伤武学

结论：

- 比华山更全面，但纯直伤峰值略低
- 在对高防目标时，`armor_broken` 有补正价值
- 是较健康的“高伤兼一点工具”的模板

代表招式：

| 招式 | 平均部位倍率 | 直伤 EV | 控制 EV |
|---|---:|---:|---:|
| 毒蛇吐信 | 1.245 | 76.94 | 0.00 |
| 拨草寻蛇 | 0.895 | 36.96 | 7.20 |
| 乌龙绞柱 | 1.050 | 52.26 | 6.60 |
| 绝招·回马枪 | 1.260 | 108.31 | 0.00 |

### 7.3 北辰一刀流

画像：

- 高直伤
- 节奏中上
- 控制略强于岳家枪法
- 有明显的爆发加压制风格

结论：

- 这是“高倍率 + 少量关键控制”的代表
- `小手击` 这类弱化招式，显著提高了它的综合能力
- 相比纯伤武学，容错更高

代表招式：

| 招式 | 平均部位倍率 | 直伤 EV | 控制 EV |
|---|---:|---:|---:|
| 切落 | 1.275 | 74.92 | 0.00 |
| 胴拂 | 1.060 | 53.41 | 4.12 |
| 小手击 | 0.900 | 30.37 | 12.70 |
| 绝技·一之太刀 | 1.275 | 115.86 | 0.00 |

### 7.4 乘云刀法

画像：

- 直伤稳定
- 节奏中上
- 控制较少
- 整体结构接近朴素输出武学

结论：

- 这门武学比较“老实”，主要靠倍率和少量破甲工作
- 如果以后数值体系要拉开输出档位，它很适合放在中高伤、低控制的基线区

代表招式：

| 招式 | 平均部位倍率 | 直伤 EV | 控制 EV |
|---|---:|---:|---:|
| 平步青云 | 1.290 | 80.32 | 0.00 |
| 破云见空 | 1.110 | 52.75 | 6.60 |
| 风起云涌 | 1.080 | 75.10 | 0.00 |
| 乘云裂天 | 1.275 | 103.18 | 0.00 |

### 7.5 碎浪掌

画像：

- 直伤中高
- 节奏最低
- 控制极强
- 生存联动很高

结论：

- 这是典型的“慢，但一旦命中就很恶心”的武学
- `stunned` 给它提供了非常高的实际价值
- 它的问题不是强度不够，而是慢速和强控的搭配是否让体验过于两极化

代表招式：

| 招式 | 平均部位倍率 | 直伤 EV | 控制 EV |
|---|---:|---:|---:|
| 裂潮击 | 1.057 | 57.74 | 2.00 |
| 断岳震 | 1.062 | 67.66 | 10.57 |

### 7.6 落梅剑法

画像：

- 直伤偏低
- 节奏中高
- 控制极强
- 生存联动极强

结论：

- 这门武学不是靠砍得疼，而是靠 `disarmed + bleeding`
- `寒枝点雪` 的缴械在当前引擎里近似等于一手硬控，所以价值很高
- 它是典型的功能型剑法，而不是硬爆发剑法

代表招式：

| 招式 | 平均部位倍率 | 直伤 EV | 控制 EV |
|---|---:|---:|---:|
| 寒枝点雪 | 1.042 | 46.47 | 14.66 |
| 回风斩 | 1.037 | 56.24 | 0.00 |

### 7.7 逍遥无相剑

画像：

- 直伤偏低
- 节奏第一
- 控制中高
- 生存联动很高

结论：

- 这门武学的核心不是单招数值，而是“快、花、功能多”
- 它靠高速度把中等控制频率堆成长期压制
- 如果以后测试发现高闪避轻功和它绑定后过强，优先怀疑的是速度与控制叠乘，而不是单一招式倍率

代表招式：

| 招式 | 平均部位倍率 | 直伤 EV | 控制 EV |
|---|---:|---:|---:|
| 北冥鲲吸 | 1.120 | 37.68 | 9.07 |
| 凌波微步 | 0.875 | 26.69 | 9.00 |
| 无相幻境 | 1.350 | 41.22 | 9.07 |
| 绝技·逍遥游 | 1.275 | 92.66 | 0.00 |

### 7.8 掌命判官笔

画像：

- 直伤偏低
- 节奏中高
- 控制手段多
- 生存联动中等

结论：

- 这门武学的特点是“工具箱型”
- 单项都不是顶级，但状态覆盖面很广
- 在长线战里，它的表现会比单看面板更好

代表招式：

| 招式 | 平均部位倍率 | 直伤 EV | 控制 EV |
|---|---:|---:|---:|
| 朱笔勾魂 | 0.885 | 21.07 | 6.35 |
| 画地为牢 | 0.880 | 27.20 | 9.05 |
| 生死立判 | 1.140 | 51.57 | 4.12 |
| 阎罗点卯 | 1.350 | 21.89 | 12.10 |

### 7.9 追影腿

画像：

- 直伤最低
- 节奏很高
- 控制第一
- 生存联动第一

结论：

- 追影腿不是伤害武学，而是典型的节奏破坏武学
- 它靠 `slowed + stunned` 拉开行动差和防守差
- 如果未来版本想保留它的特色，就不该硬补直伤；应该保持“低伤高控高节奏”的定位

代表招式：

| 招式 | 平均部位倍率 | 直伤 EV | 控制 EV |
|---|---:|---:|---:|
| 扫叶势 | 1.020 | 40.33 | 6.51 |
| 穿云踢 | 1.042 | 50.04 | 9.56 |

## 8. 术语表

给外部人或其他 AI 时，至少要解释清楚这些词：

| 术语 | 含义 |
|---|---|
| `damage_multiplier` | 招式固定倍率 |
| `power_min / power_max` | 招式浮动倍率区间，评估时可取平均值 |
| `target_bias` | 对五个基础部位的长期倾向修正 |
| `target_weights` | 直接写死到具体命中描述上的部位权重 |
| `part_bonus` | 命中特定部位时，额外提高状态触发率 |
| `part_guard` | 内功对特定部位的减伤倍率 |
| `weapon_ready` | 是否持械就绪；`disarmed` 会让它变成 `False` |
| `regeneration` | 回合开始回血 |
| `thorns` | 受击反震 |
| `vampirism` | 造成伤害后吸血 |
| `stunned` | 目标下回合直接空过 |
| `disarmed` | 目标下回合先捡武器，实战上近似空过一手 |
| `bleeding` | 回合开始持续掉血 |
| `weakened` | 降低攻击 |
| `slowed` | 降低速度 |
| `armor_broken` | 降低防御 |

## 9. 使用建议

如果后续继续做数值平衡，建议遵守这几个原则：

- 不要只看面板乘区，必须把部位、速度、状态一起看
- 不要把 `crt` 当成核心调节点，它当前影响远小于 `atk / spd / eva`
- 高速武学如果再给高硬控，强度会被行动条机制放大
- 低直伤武学未必弱，只要控制和节奏收益足够高，实战可能更强
- 任何新武学上线前，至少要用“直伤 / 节奏 / 控制 / 生存联动”四维做一次画像

## 10. 本文档的定位

这份文档是“工程化说明书”，不是最终真理。

它的价值在于：

- 比旧公式更接近当前引擎
- 比纯模拟器输出更容易解释
- 可以作为后续改动时的统一对照口径

如果未来 `engine.py` 的命中、伤害、状态、行动条规则发生变化，这份文档也应同步更新。
